pipeline {
    agent any

    tools {
        maven 'maven-home'
    }
    stages {
        stage('Build maven') {
            dir('./micro-user') {
                steps {
                    sh 'mvn --version'
                    sh 'java -version'
                    sh 'mvn clean install'
                }
            }
        }
    }
}